
@{
    ViewBag.Title = "Index6";
}

<h2>Index6</h2>
<div class="row">
    <div class="col-md-8">
        @using (Html.BeginForm("Index7", "Home", FormMethod.Post, new { id = "form1" }))
        {
            <fieldset class="scheduler-border">
                <legend class="scheduler-border" >Leveransadress</legend>
                <p>@Html.TextBox("Textruta")</p>
                <p><input name="mybutton" value="Tryck" type="submit" class="btn btn-default" /></p>

            </fieldset>
        }
    </div>
    <div class="col-md-8">
        @using (Html.BeginForm("Index7", "Home", FormMethod.Post, new { id = "form1" }))
        {
           <fieldset class="scheduler-border">
                <legend class="scheduler-border" >Fakturaadress</legend>
                <p>@Html.TextBox("Textruta2")</p>
                <p><input name="mybutton2" value="Tryck" type="submit" class="btn btn-default" /></p>

           </fieldset>
        }
    </div>
</div>

